feat(chat): 产物标题落库改为产出真实标题（画廊/预览/下载名同源受益）

此前 artifact.title 落库写死「{专家}结果」——右侧画廊、产物卡、
预览弹窗、下载文件名全部显示通用名，无法区分同类产物。

- generic 落库处提取真实标题（title 单一真相源在 artifact 表，
  展示层不重复推导）：优先产出里第一个 markdown 标题行；无标题时
  首行仅当短（≤40 字符）且无句读才采用——模型过渡句（完整长句，
  如 "I have sufficient information. Let me compile…"，E2E 实抓）
  必须兜底「{专家}结果」
- artifact.generated 事件 title 同源；画廊卡片已有 line-clamp-2
  截断，前端零改动
- 提取规则 4 条单测钉住（标题优先/过渡句兜底/短短语采用/空兜底）

E2E 验证：Vite 轮产物标题「Vite 8 新特性调研报告」「Vite 8 核心
新特性速览」在画廊与消息卡全部生效。存量产物保持原样不回填
（历史数据，预览弹窗可见内容，无兼容负担）。
测试：后端 498 绿（提取规则 +4）。

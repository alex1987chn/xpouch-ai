fix(migrate): 生产部署炸于 extra_data 列型漂移（text vs json）——迁移列型无关 + 漂移归一

生产实抓：v3.5.6 部署 backend unhealthy，日志为
`operator does not exist: text ->> unknown`（000500 的 JSON 操作符）。
根因=schema 漂移：message.extra_data 在早期 create_all 时代建为 text，
代码改注 JSON 后从未有迁移转换存量列（本地开发库=json、生产=text），
时间线统计 API 的 JSON 查询在生产同样会炸（潜伏）。

三层修复：
- 000500/000600 列类型无关化：取值统一 NULLIF(extra_data::text,'')::json
  （两种列型通吃+空串安全）；Python 解析兼容 str/dict；回写用字符串参数
- 新 000700 治本：message.extra_data / runevent.event_data 条件归一 json
  （information_schema 探测，已是 json 跳过；空串/非对象脏行置 NULL，
  与模型 Optional 语义一致）
- 验证升级：scratch 库**复刻生产场景**——建到 000400 → 手改两列 text →
  塞僵尸行+空串行 → 跑完 000500-000700，断言收尾 failed/空串 NULL/列型
  json；本地 json 列常规链 + alembic check 零漂移

部署指引：生产重新 pull main 并重跑 deploy.sh 即可（000500 会在
alembic 重试时从 000400 断点续跑，事务性保证此前未留下半完成态）。

fix(chat): 消息卡产物横条标题收敛为与画廊同源的干净标题

用户实报：横条显示产出首行原文（summary 无清洗），模型过渡句/
长句整段糊上去很乱；右侧画廊的 artifact.title 却是清洗过的
（markdown 标题优先→短无句读→兜底"{专家}结果"）。

两处本该是同一个东西却有两套生成逻辑——收敛为单一提取规则：
- utils/title_extract.artifact_title_from_output 成为唯一实现
  （generic 本地函数挪出共用，无兼容层；测试同步改 import）
- summary 生成直接取 artifact_data["title"]（generic 构造产物时
  已提取，零重复计算）
- task.completed 事件携带权威 summary（载荷=消息终态原则），前端
  实时覆盖不再用 output 首行原文（事件未带的旧后端场景才兜底）
- 迁移 20260923_000600：存量消息 summary 同步为其产物 title
  （按 artifact_ids 首个关联；同值不写），实测全部对齐（含用户
  会话的上海攻略轮）

测试：后端 499 绿、前端 97 绿、tsc 绿；空库链 46 步 + 开发库
双口径验证，alembic check 零漂移。

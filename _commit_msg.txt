fix(chat): 批准计划后依赖不再被合并清空 + 批准文案改为专家团队表述

依赖注入修复（存量 bug，顺序重构 E2E 抓出）：
- _apply_updated_plan 的依赖保留集合用 task_id or id 取键，但前端
  回传的 updated_plan 是 TaskInfo 形态（id=db uuid、无 task_id 字段）
  ——集合全落回 uuid，而 depends_on 存的是语义 id，交集恒空 →
  依赖被清成 None → 下游任务无上游输出也无缺失警告，只能靠工具
  自救（受异步落库时序影响），按教材「不得编造」产出
  「无法执行：上游缺失」
- 修法：建集合前先经 current_task_map 把 uuid 映射回语义 id，
  映射不到（用户新增任务）才用自身 id；回归测试用真实 TaskInfo
  形态（无 task_id 字段）钉住
- E2E 复核：search → writer 依赖链，writer 直接产出总结
  （不再报缺失、无需查工具）

文案（三语）：
- planApprovedMsg「计划已批准，正在恢复执行...」→
  「计划已批准，将由专家团队协作完成」（用户指出旧文案视角生硬）

测试：后端 494 绿（TaskInfo 形态回归 +1）。

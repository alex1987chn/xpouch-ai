feat(mcp): 专家 MCP 认知通道（工具清单动态注入）+ 服务器故障隔离

用户的困惑"高德 MCP 没法注入给 search 专家"排查出三层，全部修通：

认知层（主要断点）：
- 执行框架头部动态注入【可用工具清单】——本次实际绑定的真名+
  描述+来源标注+选择指引（地点/本地生活优先 maps_*）。工具收集
  上移到 prompt 增强之前（generic 重排）。新接 MCP 服务器零教材
  改动
- 顺带根治指令层硬编码错名：指令引用 search_web/read_webpage，
  实际绑定的是 asearch_web/aread_webpage（异步版真名），模型
  此前全靠猜
- search 教材"必须使用 search_web"排他定向改为"以清单为准+
  领域工具让位"（EXPERT_DEFAULTS + 迁移 20260923_000400 下发，
  空库链+开发库双口径验证，alembic check 零漂移）

拉取层（连坐 bug，E2E 实抓）：
- MultiServerMCPClient 整体 get_tools 的 TaskGroup 并发连接里，
  ModelScope 报 [USER_NOT_IN_ORG]（key/组织失效）把整个任务组
  炸掉——高德健康却一并归零（日志只见"MCP: 0"）。改为逐服务器
  独立拉取：失败单个跳过并警告暴露，健康照常注入；缓存键 MD5
  顺带换 SHA-256

清扫：
- constants.EXPERT_PROMPTS 死代码删除（教材收敛时漏删，零消费者，
  内部全是错名）

E2E 终验（新会话"查景点和餐厅点评"）：工具绑定 20 个（基础 5+
MCP 15），search 专家全程 maps_geo→maps_around_search→
maps_text_search→maps_search_detail，未调一次网页搜索。
测试：后端 498 绿。

注意：ModelScope 的 Generate-image 服务器 key 失效（USER_NOT_IN_ORG），
需在管理台更新或停用该服务器——隔离修复后它只影响自己。

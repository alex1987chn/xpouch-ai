services:
  # PostgreSQL 数据库服务 (带 pgvector 插件，支持向量检索)
  db:
    # ✅ 使用 pgvector 镜像，与 PostgreSQL 15 版本对应，数据无缝保留
    image: pgvector/pgvector:pg15
    container_name: xpouch-postgres
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-xpouch_admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin123}
      POSTGRES_DB: ${POSTGRES_DB:-xpouch_ai}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-xpouch_admin}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network

  # 后端服务
  backend:
    build: ./backend
    container_name: xpouch-backend
    restart: always
    env_file:
      - ./backend/.env
    environment:
      - PORT=3000
      # PostgreSQL 连接串（服务器部署时）
      # Docker Compose 会自动替换服务名 db 为实际容器IP
      - DATABASE_URL=postgresql+psycopg://${POSTGRES_USER:-xpouch_admin}:${POSTGRES_PASSWORD:-admin123}@db:5432/${POSTGRES_DB:-xpouch_ai}

      # 🔥🔥🔥 [新增] 覆盖默认启动命令，强制设置 600秒 超时 🔥🔥🔥
    # 注意：这里我们显式绑定了 3000 端口，与你的 Nginx 配置保持一致
    command: >
      gunicorn 
      -k uvicorn.workers.UvicornWorker 
      main:app 
      --bind 0.0.0.0:3000 
      --workers 4 
      --timeout 600
      --keep-alive 65
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-network

  # 前端服务
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: xpouch-frontend
    restart: always
    ports:
      # 映射到宿主机的 8080 端口，配合宿主机 Nginx 使用
      - "8080:80"
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge

[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "langchain-core>=0.2.0",  # 🔥 新增：Tool 调用核心支持
    "langchain-openai>=1.1.7",
    "langchain-community>=0.3.0",  # 🔥 支持 Tavily Search 等工具
    "langchain-tavily>=0.2.0",  # 🔥 新增：Tavily 官方工具包（替代 langchain-community 中的实现）
    "tavily-python>=0.5.0",  # 🔥 新增：Tavily 联网搜索 SDK
    "langgraph>=1.0.6",
    "python-dotenv>=1.2.1",
    "sqlmodel>=0.0.31",
    "tencentcloud-sdk-python>=3.0.0",
    "uvicorn>=0.40.0",
    "gunicorn>=23.0.0",  # 新增：用于生产环境多 worker 部署
    "PyJWT>=2.8.0",
    "passlib[bcrypt]>=1.7.4",
    "bcrypt==4.0.1",
    "python-multipart>=0.0.6",
    "psycopg[binary]>=3.3.0",
    "alembic>=1.13.0",
    "pgvector>=0.3.0",  # 🔥 新增：pgvector Python 客户端，支持向量操作
]
